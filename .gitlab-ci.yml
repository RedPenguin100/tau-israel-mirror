image: node:18-alpine

cache:
  paths:
    - client/node_modules/
    - client/.next/cache/

pages:
  before_script:
    # Install packages
    - cd client
    - npm install
  script:
    # Build and export application, then move content to public folder
    - npm run build
    - npm run export     
    - cd ..
    - mkdir -p public
    - mv client/out/* public
  after_script:
    # Cleanup
    - rm -rf client/out
  artifacts:
    paths:
      - public
  only:
    - main
